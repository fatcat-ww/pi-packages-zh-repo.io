{
  "name": "pi-windows-nul-fix",
  "description_en": "Pi extension that rewrites Windows-style `> nul` redirects to `> /dev/null` before Git Bash runs them, preventing spurious `nul` files on Windows.",
  "description_zh": "Pi 扩展，在 Git Bash 运行前将 Windows 风格的 `> nul` 重定向重写为 `> /dev/null`，防止在 Windows 上产生多余的 `nul` 文件",
  "translated_at": "2026-07-03T16:10:33Z"
}
